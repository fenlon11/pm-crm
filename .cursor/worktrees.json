{
  "setup-worktree": [
    "nvm use",
    "yarn",
    "cp $ROOT_WORKTREE_PATH/packages/pm-server/.env packages/pm-server/.env || true",
    "cp $ROOT_WORKTREE_PATH/packages/pm-front/.env packages/pm-front/.env || true"
  ]
}
